import React from 'react'
import {screenSchemaType} from "@/app/admin/(authenticated)/screens/form-schema";

const ScreenForm = (
    { defaultValues }: { defaultValues: screenSchemaType }
) => {
    return (
        <div>
            ScreenForm
        </div>
    )
}
export default ScreenForm
